test:
  box: node:6.4.0
  steps:
    - npm-install
    - npm-test
    - script:
      name: lint
      code: npm run lint

coverage:
  box: node:6.4.0
  steps:
    - npm-install
    - npm-test
    - script:
      name: coverage
      code: |
        npm run coverage

release:
  box: node:6.4.0
  steps:
    - npm-install
    - script:
      name: compile
      code: tsc
    - script:
      name: npm publish
      code: npm publish
